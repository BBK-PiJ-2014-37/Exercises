int getDecimal(String str) {
	int d = (int)s.charAt(i) - (int)"0"
    char c = s.charAt(i).toLower()
    if (isDigit(c)) {
        d = (int)c - (int)"0"
    } else if (c >= "a" && c <= "f") {
    	d = 10 + (int)c - (int)"a"
	}
d = n % b
n = n / b
}

